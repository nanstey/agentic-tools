# Skill Graph

Which skills reference which. Generated by `scripts/skill-graph`;
do not edit by hand.

57 skills, 133 references. Solid arrows stay
inside a category; dotted arrows cross category boundaries. Each
diagram shows every edge touching its category.

## agent

```mermaid
graph LR
    subgraph agent
        context-engineering
        reflect
        scope
    end
    changes -.-> scope
    deep-review -.-> scope
    dry -.-> scope
    extract -.-> scope
    idea-to-pr -.-> reflect
    pr-split -.-> scope
    principles -.-> scope
    scope -.-> pr-info
```

## code-quality

```mermaid
graph LR
    subgraph code-quality
        deep-review
        principles
        scrutiny
        sniff
        extract --> dry
        extract --> srp
        refactor --> fresh-air
    end
    branch -.-> refactor
    deep-review -.-> pr-info
    deep-review -.-> scope
    dry -.-> scope
    extract -.-> scope
    idea-to-pr -.-> deep-review
    idea-to-pr -.-> principles
    new-app -.-> scrutiny
    principles -.-> prep
    principles -.-> scope
```

## dev

```mermaid
graph LR
    subgraph dev
        prototype
        branch --> build
        commit --> changes
        rebase --> branch
        rebase --> conflicts
        worktree --> branch
    end
    branch -.-> refactor
    build -.-> speclist
    changes -.-> pr-info
    changes -.-> scope
    conflicts -.-> gh-stack
    flow-design -.-> build
    gh-stack -.-> conflicts
    gh-stack -.-> rebase
    idea-to-pr -.-> branch
    idea-to-pr -.-> commit
    idea-to-pr -.-> worktree
    plan-init -.-> build
    pr -.-> branch
    pr -.-> changes
    pr -.-> commit
    pr-rebase -.-> conflicts
    pr-rebase -.-> rebase
    pr-screenshots -.-> worktree
    pr-split -.-> changes
    product-review -.-> prototype
    visual-capture -.-> worktree
```

## env

```mermaid
graph LR
    subgraph env
        new-app --> new-repo
        new-repo --> new-app
        new-repo --> orca-repo
        new-repo --> worktree-env
        orca-repo --> new-app
        orca-repo --> new-repo
    end
    new-app -.-> proposal
    new-app -.-> scrutiny
```

## issues

```mermaid
graph LR
    subgraph issues
        issue-create
    end
    issue-create -.-> pr-info
```

## planning

```mermaid
graph LR
    subgraph planning
        plan-init --> product-review
        plan-init --> program-design
        plan-init --> proposal
        plan-init --> system-architecture
        plan-init --> vertical-slices
        product-review --> plan-init
        product-review --> system-architecture
        program-design --> plan-init
        program-design --> vertical-slices
        proposal --> plan-init
        proposal --> product-review
        proposal --> program-design
        proposal --> system-architecture
        proposal --> vertical-slices
        speclist --> plan-init
        speclist --> vertical-slices
        system-architecture --> plan-init
        system-architecture --> program-design
        vertical-slices --> plan-init
        vertical-slices --> speclist
    end
    build -.-> speclist
    idea-to-pr -.-> plan-init
    idea-to-pr -.-> product-review
    idea-to-pr -.-> program-design
    idea-to-pr -.-> proposal
    idea-to-pr -.-> speclist
    idea-to-pr -.-> system-architecture
    idea-to-pr -.-> vertical-slices
    new-app -.-> proposal
    plan-init -.-> build
    product-review -.-> grow
    product-review -.-> prep
    product-review -.-> prototype
    proposal -.-> grow
```

## pull-requests

```mermaid
graph LR
    subgraph pull-requests
        gh-stack --> pr-restack
        pr --> pr-create
        pr --> pr-info
        pr --> pr-monitor
        pr-ci --> pr-info
        pr-comments --> pr-info
        pr-create --> pr-description
        pr-create --> pr-info
        pr-description --> pr-create
        pr-description --> pr-info
        pr-description --> pr-screenshots
        pr-info --> gh-stack
        pr-monitor --> gh-stack
        pr-monitor --> pr-ci
        pr-monitor --> pr-comments
        pr-monitor --> pr-description
        pr-monitor --> pr-info
        pr-monitor --> pr-rebase
        pr-rebase --> gh-stack
        pr-rebase --> pr-info
        pr-restack --> gh-stack
        pr-screenshots --> pr
        pr-screenshots --> pr-create
        pr-screenshots --> pr-description
        pr-screenshots --> pr-info
        pr-split --> gh-stack
        pr-split --> pr-create
        pr-split --> pr-info
        pr-split --> pr-restack
    end
    changes -.-> pr-info
    conflicts -.-> gh-stack
    deep-review -.-> pr-info
    gh-stack -.-> conflicts
    gh-stack -.-> rebase
    idea-to-pr -.-> gh-stack
    idea-to-pr -.-> pr
    idea-to-pr -.-> pr-restack
    issue-create -.-> pr-info
    pr -.-> branch
    pr -.-> changes
    pr -.-> commit
    pr-comments -.-> terse
    pr-description -.-> bluf
    pr-description -.-> terse
    pr-rebase -.-> conflicts
    pr-rebase -.-> rebase
    pr-screenshots -.-> before-after-bridge
    pr-screenshots -.-> playwright-cli
    pr-screenshots -.-> visual-capture
    pr-screenshots -.-> worktree
    pr-split -.-> changes
    pr-split -.-> scope
    scope -.-> pr-info
    visual-capture -.-> pr
    visual-capture -.-> pr-description
    visual-capture -.-> pr-screenshots
```

## tools

```mermaid
graph LR
    subgraph tools
        chrome-devtools-cli
        playwright-cli
    end
    idea-to-pr -.-> playwright-cli
    pr-screenshots -.-> playwright-cli
    visual-capture -.-> playwright-cli
```

## visual

```mermaid
graph LR
    subgraph visual
        visual-capture
    end
    idea-to-pr -.-> visual-capture
    pr-screenshots -.-> visual-capture
    visual-capture -.-> before-after-bridge
    visual-capture -.-> playwright-cli
    visual-capture -.-> pr
    visual-capture -.-> pr-description
    visual-capture -.-> pr-screenshots
    visual-capture -.-> worktree
```

## workflow

```mermaid
graph LR
    subgraph workflow
        flow-design --> idea-to-pr
        idea-to-pr --> flow-design
    end
    flow-design -.-> build
    idea-to-pr -.-> branch
    idea-to-pr -.-> commit
    idea-to-pr -.-> deep-review
    idea-to-pr -.-> gh-stack
    idea-to-pr -.-> plan-init
    idea-to-pr -.-> playwright-cli
    idea-to-pr -.-> pr
    idea-to-pr -.-> pr-restack
    idea-to-pr -.-> principles
    idea-to-pr -.-> product-review
    idea-to-pr -.-> program-design
    idea-to-pr -.-> proposal
    idea-to-pr -.-> reflect
    idea-to-pr -.-> speclist
    idea-to-pr -.-> system-architecture
    idea-to-pr -.-> vertical-slices
    idea-to-pr -.-> visual-capture
    idea-to-pr -.-> worktree
```

## writing

```mermaid
graph LR
    subgraph writing
        observation-impact-question
        before-after-bridge --> bluf
        grow --> bluf
        grow --> prep
        prep --> bluf
        write-docs --> bluf
        write-docs --> deslop
        write-docs --> terse
    end
    pr-comments -.-> terse
    pr-description -.-> bluf
    pr-description -.-> terse
    pr-screenshots -.-> before-after-bridge
    principles -.-> prep
    product-review -.-> grow
    product-review -.-> prep
    proposal -.-> grow
    visual-capture -.-> before-after-bridge
```
